<h2>Account Activation</h2>
<%= simple_form_for resource, :as => resource_name, :url => update_user_confirmation_path, :html => {:class => 'form-horizontal', :method => 'put'}, :id => 'activation-form' do |f| %>
  <%= devise_error_messages! %>
  <fieldset>
    <legend>
      Account Activation
      <% if resource.email %>
        for <%= resource.email %>
      <% end %>
    </legend>
    <%= f.input :honorific, collection: %w[Mr Miss Ms Mrs Prof. Dr], label: "Title"%>
    <%= f.input :first_name %>
    <%= f.input :other_names, required: false %>
    <%= f.input :last_name %>
    <%= f.input :preferred_name, required: false %>
    <% if @requires_password %>
      <%= f.input :password, label: 'Choose a Password:' %>
      <%= f.input :password_confirmation, label: 'Password Confirmation:' %>
    <% end %>
    <%= hidden_field_tag :confirmation_token, @confirmation_token %>
    <p><%= f.submit "Activate" %></p>
  </fieldset>
<% end %>